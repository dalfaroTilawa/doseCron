# ==============================================================================
# CONFIGURACIÓN DE ENTORNO - DoseCron (EJEMPLO)
# ==============================================================================
# Copiar este archivo a .env y configurar los valores según el entorno.
# Este archivo SÍ debe estar en el repositorio como documentación.

# ------------------------------------------------------------------------------
# API DE FERIADOS
# ------------------------------------------------------------------------------
# URL base del servicio de feriados (date.nager.at)
VITE_API_BASE_URL=https://date.nager.at/api/v3

# Timeout para peticiones HTTP en milisegundos (10 segundos)
VITE_API_TIMEOUT=10000

# ------------------------------------------------------------------------------
# CONFIGURACIÓN DE CACHE
# ------------------------------------------------------------------------------
# Tiempo de vida del cache en milisegundos (30 días = 2592000000 ms)
VITE_CACHE_TTL=2592000000

# Prefijo para las claves de localStorage
VITE_STORAGE_PREFIX=dosecron

# ------------------------------------------------------------------------------
# CONFIGURACIÓN DE LA APLICACIÓN
# ------------------------------------------------------------------------------
# Máximo número de iteraciones para cálculo de fechas
VITE_MAX_ITERATIONS=1000

# Máximo número de intentos para encontrar día válido
VITE_MAX_ATTEMPTS=30

# ------------------------------------------------------------------------------
# CONFIGURACIONES POR DEFECTO DEL USUARIO
# ------------------------------------------------------------------------------
# País por defecto (código ISO de 2 letras)
VITE_DEFAULT_COUNTRY=CR

# Tema por defecto (light/dark)
VITE_DEFAULT_THEME=light

# Idioma por defecto (código ISO de 2 letras)
VITE_DEFAULT_LANGUAGE=es

# Unidad de duración por defecto (days/weeks/months/years)
VITE_DEFAULT_DURATION_UNIT=months

# Excluir fines de semana por defecto (true/false)
VITE_DEFAULT_EXCLUDE_WEEKENDS=true

# Excluir feriados por defecto (true/false)
VITE_DEFAULT_EXCLUDE_HOLIDAYS=true

# ------------------------------------------------------------------------------
# CONFIGURACIÓN DE DESARROLLO
# ------------------------------------------------------------------------------
# Habilitar modo debug (true/false)
# En modo debug se muestran logs adicionales en la consola
VITE_DEBUG_MODE=false

# Habilitar modo desarrollador (true/false)
# En modo desarrollador se muestran estadísticas de uso en pantalla
VITE_DEVELOPER_MODE=false

# Nivel de logs (error, warn, info, debug)
# error: solo errores críticos
# warn: errores y advertencias
# info: información general + warn + error
# debug: todos los logs (solo si DEBUG_MODE=true)
VITE_LOG_LEVEL=warn

# ==============================================================================
# NOTAS IMPORTANTES:
# ==============================================================================
# 1. El prefijo VITE_ es obligatorio para que Vite exponga las variables al frontend
# 2. Las variables booleanas deben ser 'true' o 'false' (strings)
# 3. Los números deben ser strings que se convierten automáticamente
# 4. Los cambios en .env requieren reiniciar el servidor de desarrollo
# 5. NUNCA commitear el archivo .env real con valores sensibles